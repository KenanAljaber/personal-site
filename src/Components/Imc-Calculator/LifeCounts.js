import "../../datePicker.css"
const Lifecounts = ({text,number}) => {
    return (  
        <div className="counter">
            <p>{text}</p>
            <p>{number}</p>

        </div>
    );
}
 
export default Lifecounts;
